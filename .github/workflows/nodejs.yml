name: Node.js CI

on:
  push:
    branches:
      - main

jobs:
  lintTest:
    name: Lint and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: Set Working Directory
        run: |
          echo "Setting working directory..."
          cd ${{ github.workspace }}
          ls
          cd backend

      - name: Use Node.js 14.x
        uses: actions/setup-node@v2
        with:
          node-version: 14.x

      - name: Install Dependencies
        run: |
          echo "Installing dependencies"
          ls
          cd backend 
          npm install
      # - name: Run Tests
      #   run: |
      #     echo "Running test files.."
      #     ls
      #     cd backend
      #     npm test
      - name: Build
        run: |
          eco "Building dependencies.."
          ls
          cd backend 
          npm run build
        env:
          CI: true
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v2

#       - name: Setup Node.js
#         run: |
#           echo "Setting working directory..."
#           cd ${{ github.workspace }}
#           ls
#           cd backend
#           ls
#           uses: actions/setup-node@v4
#           with:
#           node-version: "14"

#       - name: Install Dependencies
#         run: npm install

#       - name: Run Tests
#         run: npm run test

#       - name: Build
#         run: npm run build

#       - name: Finalize
#         run: echo "Successfully completed CI."

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build:
#     steps: # Add this line
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v2

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "14"

#       - name: Install Dependencies
#         run: npm install

#        -name: Finalize
#         run: echo "Successfully completed CI."

#       # - name: Run Tests
#       #   run: npm test
